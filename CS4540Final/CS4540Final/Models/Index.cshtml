@page
@model CS4540Final.Models.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HighScore[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HighScore[0].Time)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.HighScore) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Time)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.HighScoreID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.HighScoreID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.HighScoreID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
